import { TopbarLinkDirective } from './topbar-link.directive';

describe('TopbarLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new TopbarLinkDirective();
    expect(directive).toBeTruthy();
  });
});
